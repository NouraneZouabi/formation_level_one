apiVersion: v1
kind: Service
metadata: 
  name: spring-boot-app-service
  labels:
    app: spring-boot-app
spec:
  type: ClusterIP
  selector:
    app: spring-boot-app
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081


